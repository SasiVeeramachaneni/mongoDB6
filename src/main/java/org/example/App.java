package org.example;

/**
 * Hello world!
 */
public class App {
    public static void main(String[] args) {

        /*//to Get the list of Events
        processEvents myEvents = new processEvents(new Connection().getConnection());

        Bson bson = myEvents.getEvent(557378);
        System.out.println(bson);
        Document document = (Document) bson;
        System.out.println((int) document.get("account_id"));*/


        //To delete the event from DB
        //myEvents.deleteEvent(557378);
    }
}
